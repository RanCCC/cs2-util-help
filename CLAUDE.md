# cs2-util-help Development Guidelines

Auto-generated from all feature plans. Last updated: 2026-04-05

## Active Technologies

- JavaScript (ES2022+) + React 19, Express, Vite (001-cs2-utility-guide)

## Project Structure

```text
server.js          # Express backend (serves frontend + REST API)
src/               # React frontend source
  pages/           # HomePage, MapPage, DetailPage
  components/      # MapCanvas, CategoryPanel, MarkerOverlay, LineupForm, ExportDialog, ImportDialog
  services/        # api.js, compression.js, zipService.js
data/              # Lineup data and assets (JSON configs + media files)
tests/             # Vitest — unit, integration, component tests
dist/              # Built frontend (generated by npm run build)
start.bat          # Windows launcher for non-tech users
```

## Commands

```bash
npm install          # Install dependencies
npm run dev          # Start Vite dev server (frontend)
npm run dev:server   # Start Express backend in dev mode
npm run build        # Build frontend to dist/
npm start            # Start production server (serves built frontend + API)
npm test             # Run tests with Vitest
```

## Code Style

JavaScript: ES modules, CSS Modules for styling, named constants for configuration values.
Follow constitution principles (`.specify/memory/constitution.md`).

## Key Dependencies

- react, react-dom, react-router-dom — Frontend framework + routing
- express — Backend server for file system access
- fflate — ZIP creation/reading (export/import)
- browser-image-compression — Client-side image compression to WebP
- @ffmpeg/ffmpeg, @ffmpeg/util — Client-side video compression to WebM/VP9

## Recent Changes

- 001-cs2-utility-guide: Added React 19 + Express stack for CS2 utility lineup guide

<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
